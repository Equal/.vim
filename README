Don't forget to set

so ~/.vim/versioned_vimrc.vim

in your .vimrc

After cloning, cd to .vim and run git submodule update --init.
Open up an install of vim and run the :BundleInstall command

credit to MattCL for the idea to source control
